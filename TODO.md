- [ ] **Loading state for calendars** - When `isLoading` is true, calendars show "No history data" instead of a skeleton/loading indicator.

- [ ] **Clear selection button** - When items are selected, add a way to clear all selections without clicking each one.

- [ ] **URL validation in MomentContent** - `new URL(item.url)` can throw if URL is malformed. Wrap in try/catch.
